<%= form_for(@article, :html => {:id => "press_articles_input", :style => "display: none"}, remote: true)  do |f| %>

 <!-- html: { method: :delete } -->
	<%= f.text_field :url %>
	<%= f.hidden_field :ghost_id, :value => "z" %>
	<%= f.submit :class => "button" %>
<% end %>

