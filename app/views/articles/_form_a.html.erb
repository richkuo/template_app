<%= form_for(@article, :html => {:id => "press_articles_input_a", :style => "display: none"}, remote: true)  do |f| %>

 <!-- html: { method: :delete } -->
	<%= f.text_field :url, :value => "" %>
	<%= f.hidden_field :ghost_id, :value => "a" %>
	<%= f.submit :class => "button", :id =>"button_a" %>
<% end %>

